# Node modules
node_modules/

# Build output (www is generated by npm run build)
# Keep www tracked so the Android build includes web assets

# Android build artifacts are handled in android/.gitignore

# Excel template files (large binary files)
*.xlsx

# Environment / secrets
.env
.env.local
google-services.json
